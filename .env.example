# Environment Variables Template
# Copy this file to .env and fill in your values
# NEVER commit .env to git

# Node Environment
NODE_ENV=development

# Server Configuration
PORT=5000

# Database Configuration (if using PostgreSQL)
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Firebase Configuration (Server-side)
FIREBASE_PROJECT_ID=royalcarriagelimoseo

# Firebase Client Configuration (for Admin Dashboard - NEXT_PUBLIC_* are exposed to client)
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=royalcarriagelimoseo.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=royalcarriagelimoseo
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=royalcarriagelimoseo.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-firebase-app-id
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=your-ga-measurement-id

# Google Cloud AI Configuration
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# AI API Keys (Optional - for external AI services)
# OPENAI_API_KEY=your-openai-api-key

# Analytics Configuration (Optional)
# GOOGLE_ANALYTICS_PROPERTY_ID=your-ga-property-id

# Disable Astro telemetry (set to 1 to opt out of sending anonymous build data)
# Set in your environment or CI to prevent calls to telemetry.astro.build
ASTRO_TELEMETRY_DISABLED=1

# Anthropic / Claude API (optional)
# If you use Anthropic's Claude, set the API key in your environment or CI.
# Example (do NOT commit real keys):
# CLAUDE_API_KEY=your-claude-api-key


# Admin Access (Optional - for initial setup)
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=secure-password

# Add other environment variables as needed
