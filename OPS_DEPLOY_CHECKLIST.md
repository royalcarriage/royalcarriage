# Admin Dashboard Deployment

This document outlines the steps to deploy the admin dashboard.

## Deployment Checklist

- [ ] Confirm Firebase project is active (`firebase use default`).
- [ ] Build the admin dashboard (`npm run build`).
- [ ] Deploy to Firebase (`npm run deploy`).
